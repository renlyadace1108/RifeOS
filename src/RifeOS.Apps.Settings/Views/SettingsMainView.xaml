<UserControl x:Class="RifeOS.Apps.Settings.Views.SettingsMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:RifeOS.Apps.Settings.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:SettingsViewModel}"
             Background="{DynamicResource BackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="32">
        <StackPanel MaxWidth="700" HorizontalAlignment="Left">
            <TextBlock Text="⚙ 系统设置" FontSize="26" FontWeight="ExtraBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,6"/>
            <TextBlock Text="配置 RifeOS 运行环境、个性化主题与用户画像" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" Opacity="0.6" Margin="0,0,0,24"/>

            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="界面外观与配色" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,12"/>
                    <TextBlock Text="选择适合你的工作台显示模式（黑灰质感 / 白灰极简）" FontSize="12" Foreground="{DynamicResource ForegroundBrush}" Opacity="0.7" Margin="0,0,0,14"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Command="{Binding SetThemeCommand}" CommandParameter="Dark" Margin="0,0,8,0" Padding="0,14" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="🌙" FontSize="22" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                                <TextBlock Text="深色模式 (Dark)" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="1" Command="{Binding SetThemeCommand}" CommandParameter="Light" Margin="8,0,0,0" Padding="0,14" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="☀️" FontSize="22" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                                <TextBlock Text="浅色模式 (Light)" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="用户凭证与身份" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,14"/>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="用户昵称" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" Opacity="0.7"/>
                        <TextBlock Text="{Binding User.Nickname}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="系统标识 (User ID)" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" Opacity="0.7"/>
                        <TextBlock Text="{Binding User.UserId}" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid>
                        <TextBlock Text="个人签名" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" Opacity="0.7"/>
                        <TextBlock Text="{Binding User.Signature}" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Right"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="关于 RifeOS" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,14"/>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="系统版本" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" Opacity="0.7"/>
                        <TextBlock Text="{Binding SystemVersion}" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="内核架构" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" Opacity="0.7"/>
                        <TextBlock Text="{Binding RuntimeArchitecture}" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid>
                        <TextBlock Text="系统设计与开发者" FontSize="13" Foreground="{DynamicResource ForegroundBrush}" Opacity="0.7"/>
                        <TextBlock Text="{Binding Author}" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" HorizontalAlignment="Right"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
