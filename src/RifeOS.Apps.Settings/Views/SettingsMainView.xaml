<UserControl x:Class="RifeOS.Apps.Settings.Views.SettingsMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackgroundBrush}"
             Foreground="{DynamicResource TextPrimaryBrush}"
             FontFamily="Segoe UI, Microsoft YaHei UI">
	<ScrollViewer VerticalScrollBarVisibility="Auto" Padding="36,28">
		<StackPanel MaxWidth="720" HorizontalAlignment="Left">
			<TextBlock Text="系统设置" FontSize="22" FontWeight="Bold" Margin="0,0,0,24"/>

			<!-- 1. 个性化与主题设置面板 -->
			<Border Background="{DynamicResource SurfaceBackgroundBrush}" CornerRadius="10"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="20" Margin="0,0,0,16">
				<StackPanel>
					<StackPanel Orientation="Horizontal" Margin="0,0,0,12">
						<TextBlock Text="🎨" FontSize="14" Margin="0,0,8,0"/>
						<TextBlock Text="外观与个性化" FontSize="14" FontWeight="SemiBold"/>
					</StackPanel>

					<TextBlock Text="选择 RifeOS 桌面工作台的主题样式：" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,12"/>

					<StackPanel Orientation="Horizontal">
						<Button Content="Dark (默认深色)" Height="34" Padding="14,0"
                                Background="{DynamicResource SubSurfaceBackgroundBrush}"
                                Foreground="{DynamicResource TextPrimaryBrush}"
                                BorderThickness="1" BorderBrush="#3B82F6" Cursor="Hand" Margin="0,0,10,0"
                                Command="{Binding ChangeThemeCommand}" CommandParameter="Dark (默认深色)"/>

						<Button Content="OLED Midnight" Height="34" Padding="14,0"
                                Background="#000000" Foreground="#CCCCCC"
                                BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Cursor="Hand" Margin="0,0,10,0"
                                Command="{Binding ChangeThemeCommand}" CommandParameter="OLED Midnight"/>

						<Button Content="Light (浅色)" Height="34" Padding="14,0"
                                Background="#E5E7EB" Foreground="#111827"
                                BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Cursor="Hand"
                                Command="{Binding ChangeThemeCommand}" CommandParameter="Light (浅色)"/>
					</StackPanel>
				</StackPanel>
			</Border>

			<!-- 2. 独立沙箱与存储信息 -->
			<Border Background="{DynamicResource SurfaceBackgroundBrush}" CornerRadius="10"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="20">
				<StackPanel>
					<StackPanel Orientation="Horizontal" Margin="0,0,0,8">
						<TextBlock Text="💾" FontSize="14" Margin="0,0,8,0"/>
						<TextBlock Text="沙箱隔离存储" FontSize="14" FontWeight="SemiBold"/>
					</StackPanel>
					<TextBlock Text="{Binding StorageUsage}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
				</StackPanel>
			</Border>
		</StackPanel>
	</ScrollViewer>
</UserControl>